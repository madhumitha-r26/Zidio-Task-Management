import React from "react";

const Logout = () => {

  return (
    <div>
      <button className="btn btn-outline btn-primary">
        Logout
      </button>
    </div>
  );
};

export default Logout;
